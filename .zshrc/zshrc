#
#
. "${HOME}/.zshrc-utils/-loggers.sh"
. "${HOME}/.zshrc-utils/-utils-alias.sh"
. "${HOME}/.zshrc-utils/-utils-go-2.sh"
. "${HOME}/.zshrc-utils/-utils-sleep.sh"
. "${HOME}/.zshrc-utils/-utils-java.sh"
. "${HOME}/.zshrc-utils/-utils-zsh-completions.sh"

export NVM_DIR=~/.nvm
source $(brew --prefix nvm)/nvm.sh

### MANAGED BY RANCHER DESKTOP START (DO NOT EDIT)
export PATH="/Users/lego/.rd/bin:$PATH"
### MANAGED BY RANCHER DESKTOP END (DO NOT EDIT)

### ANDROID_STUDIO_VARIABLES - START
export ANDROID_HOME=/Users/lego/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
### ANDROID_STUDIO_VARIABLES - END

# Add RVM to PATH for scripting. Make sure this is the last PATH variable change.
export PATH="$PATH:$HOME/.rvm/bin"
